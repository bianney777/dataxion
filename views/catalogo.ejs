<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Catálogo Agrícola</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.0/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/css/gestionfinca.css">
  <script src="/js/app.js" defer></script>
  <script src="/js/icons.js" defer></script>
</head>
<body>
  <%- include('partial/header') %>
  <div class="layout">
    <%- include('partial/sidebar') %>
    <div class="feed">
      <section class="catalog-hero mb-10">
        <div class="hero-bg"></div>
        <div class="hero-content">
          <div class="hero-text">
            <h1>Catálogo Agrícola</h1>
            <p class="hero-sub">Explora y gestiona categorías de cultivos e insumos con una vista estilo marketplace. Haz click en una tarjeta para ver detalles enriquecidos.</p>
            <div class="hero-stats">
              <div class="hstat"><span class="v"><%= categoriasCultivo.length %></span><span class="l">Cat. Cultivo</span></div>
              <div class="hstat"><span class="v"><%= Object.values(tiposPorCategoria).reduce((a,b)=>a+b.length,0) %></span><span class="l">Tipos</span></div>
              <div class="hstat"><span class="v"><%= categoriasInsumo.length %></span><span class="l">Cat. Insumo</span></div>
              <div class="hstat"><span class="v"><%= insumos.length %></span><span class="l">Insumos</span></div>
            </div>
            <div class="hero-actions">
              <button class="btn btn-success btn-sm" data-open-modal="createCategoriaCultivo">+ Cat. Cultivo</button>
              <button class="btn btn-success btn-sm" data-open-modal="createTipoCultivo">+ Tipo</button>
              <button class="btn btn-success btn-sm" data-open-modal="createVariedadCultivo">+ Variedad</button>
              <button class="btn btn-warning btn-sm" data-open-modal="createCategoriaInsumo">+ Cat. Insumo</button>
              <button class="btn btn-warning btn-sm" data-open-modal="createInsumo">+ Insumo</button>
            </div>
          </div>
        </div>
      </section>

      <div class="glass-card rounded-xl p-4 mb-6 flex flex-wrap gap-2 items-center justify-between border border-slate-200/60">
        <div class="flex gap-2 flex-wrap">
          <button class="btn btn-success btn-xs" data-open-modal="createCategoriaCultivo">+ Cat. Cultivo</button>
          <button class="btn btn-success btn-xs" data-open-modal="createTipoCultivo">+ Tipo Cultivo</button>
          <button class="btn btn-success btn-xs" data-open-modal="createVariedadCultivo">+ Variedad</button>
          <div class="w-px h-6 bg-slate-200"></div>
          <button class="btn btn-warning btn-xs" data-open-modal="createCategoriaInsumo">+ Cat. Insumo</button>
          <button class="btn btn-warning btn-xs" data-open-modal="createInsumo">+ Insumo</button>
        </div>
        <div class="text-[10px] font-medium uppercase tracking-wider text-slate-400">CRUD en vivo</div>
      </div>

      <div class="glass-card rounded-xl p-4 mb-6 flex flex-col md:flex-row gap-4 md:items-end">
        <div class="flex flex-col w-full md:w-1/3">
          <label class="text-[11px] font-semibold uppercase tracking-wider text-slate-500 mb-1">Buscar</label>
          <input id="catalogSearch" type="text" placeholder="Buscar cultivo, variedad o insumo..." class="input-modern">
        </div>
        <div class="flex flex-col w-full md:w-1/3">
          <label class="text-[11px] font-semibold uppercase tracking-wider text-slate-500 mb-1">Filtrar tipo</label>
          <select id="catalogFilter" class="input-modern">
            <option value="">Todo</option>
            <option value="cultivo">Cultivos</option>
            <option value="insumo">Insumos</option>
          </select>
        </div>
        <div class="flex gap-2 pt-2 md:pt-0">
          <button id="expandAllBtn" class="btn btn-xs btn-outline">Expandir</button>
          <button id="collapseAllBtn" class="btn btn-xs btn-outline">Colapsar</button>
        </div>
      </div>

      <%- include('partial/catalog_unified') %>

    </div>
    <aside class="rightbar">
      <div class="right-card">
        <h4 class="text-xs font-semibold tracking-wider text-slate-500 uppercase">Tips</h4>
        <p class="text-[11px] leading-relaxed">Usa el buscador para filtrar rápidamente cultivos o insumos. Próxima mejora: carga diferida de variedades.</p>
      </div>
    </aside>
  </div>
  <!-- Lógica de filtrado movida a app.js (catalogInit) -->
  <div id="modalHost" style="display:none"></div>
  <%- include('partial/footer') %>
</body>
</html>

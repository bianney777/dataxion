<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Catálogo Agrícola</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.0/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/css/gestionfinca.css">
  <script src="/js/app.js" defer></script>
</head>
<body>
  <%- include('partial/header') %>
  <div class="layout">
    <%- include('partial/sidebar') %>
    <div class="feed">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-2xl font-bold bg-gradient-to-r from-emerald-600 to-lime-600 text-transparent bg-clip-text tracking-tight">Catálogo Agrícola</h2>
        <div class="flex gap-2 text-[11px] font-medium">
          <span class="metric-chip">Categorías cultivo: <%= categoriasCultivo.length %></span>
          <span class="metric-chip">Tipos cultivo: <%= Object.values(tiposPorCategoria).reduce((a,b)=>a+b.length,0) %></span>
          <span class="metric-chip">Categorías insumo: <%= categoriasInsumo.length %></span>
          <span class="metric-chip">Insumos: <%= insumos.length %></span>
        </div>
      </div>

      <div class="glass-card rounded-xl p-4 mb-6 flex flex-wrap gap-2 items-center justify-between border border-slate-200/60">
        <div class="flex gap-2 flex-wrap">
          <button class="btn btn-success btn-xs" data-open-modal="createCategoriaCultivo">+ Cat. Cultivo</button>
          <button class="btn btn-success btn-xs" data-open-modal="createTipoCultivo">+ Tipo Cultivo</button>
          <button class="btn btn-success btn-xs" data-open-modal="createVariedadCultivo">+ Variedad</button>
          <div class="w-px h-6 bg-slate-200"></div>
          <button class="btn btn-warning btn-xs" data-open-modal="createCategoriaInsumo">+ Cat. Insumo</button>
          <button class="btn btn-warning btn-xs" data-open-modal="createInsumo">+ Insumo</button>
        </div>
        <div class="text-[10px] font-medium uppercase tracking-wider text-slate-400">CRUD en vivo</div>
      </div>

      <div class="glass-card rounded-xl p-4 mb-6 flex flex-col md:flex-row gap-4 md:items-end">
        <div class="flex flex-col w-full md:w-1/3">
          <label class="text-[11px] font-semibold uppercase tracking-wider text-slate-500 mb-1">Buscar</label>
          <input id="catalogSearch" type="text" placeholder="Buscar cultivo, variedad o insumo..." class="input-modern">
        </div>
        <div class="flex flex-col w-full md:w-1/3">
          <label class="text-[11px] font-semibold uppercase tracking-wider text-slate-500 mb-1">Filtrar tipo</label>
          <select id="catalogFilter" class="input-modern">
            <option value="">Todo</option>
            <option value="cultivo">Cultivos</option>
            <option value="insumo">Insumos</option>
          </select>
        </div>
        <div class="flex gap-2 pt-2 md:pt-0">
          <button id="expandAllBtn" class="btn btn-xs btn-outline">Expandir</button>
          <button id="collapseAllBtn" class="btn btn-xs btn-outline">Colapsar</button>
        </div>
      </div>

      <div class="mb-8">
        <h3 class="text-xs font-semibold uppercase tracking-wider text-slate-500 mb-3 flex items-center gap-2">Cultivos <span class="live-badge">LIVE</span></h3>
        <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3" id="cultivosGrid">
          <% categoriasCultivo.forEach(cat => { const tipos = tiposPorCategoria[cat.id_categoria] || []; %>
            <div class="glass-card rounded-xl p-5 flex flex-col gap-3 border border-slate-200/60 relative group" data-kind="cultivo" data-name="<%= cat.nombre_categoria %>" data-entity="categoria-cultivo" data-id="<%= cat.id_categoria %>">
              <div class="flex justify-between items-start">
                <div>
                  <h4 class="font-semibold text-slate-800 group-hover:text-emerald-600 transition flex items-center gap-2"><%= cat.nombre_categoria %></h4>
                  <p class="text-[11px] text-slate-500 mt-1"><%= cat.descripcion || 'Sin descripción.' %></p>
                </div>
                <div class="flex flex-col items-end gap-2">
                  <span class="status-badge status-activa"><%= tipos.length %> tipos</span>
                  <div class="flex gap-1 opacity-0 group-hover:opacity-100 transition">
                    <button class="btn btn-outline btn-xs" data-edit="categoria-cultivo" data-id="<%= cat.id_categoria %>">Editar</button>
                    <button class="btn btn-danger btn-xs" data-delete="categoria-cultivo" data-id="<%= cat.id_categoria %>">X</button>
                  </div>
                </div>
              </div>
              <% if (tipos.length) { %>
                <div class="border border-slate-200/60 rounded-lg divide-y divide-slate-200 overflow-hidden bg-white/40 backdrop-blur-sm">
                  <% tipos.forEach(t => { %>
                    <div class="p-3 flex flex-col gap-1 group/item" data-kind="cultivo" data-name="<%= t.nombre_tipo %>" data-entity="tipo-cultivo" data-id="<%= t.id_tipo_cultivo %>">
                      <div class="flex justify-between items-center">
                        <span class="text-[12px] font-semibold text-slate-700"><%= t.nombre_tipo %></span>
                        <% if (t.nombre_cientifico) { %><span class="text-[10px] italic text-slate-500"><%= t.nombre_cientifico %></span><% } %>
                      </div>
                      <% if (t.descripcion) { %><p class="text-[10px] text-slate-500 leading-snug"><%= t.descripcion.substring(0,140) %><%= t.descripcion.length>140?'...':'' %></p><% } %>
                      <div class="flex flex-wrap gap-1 mt-1">
                        <% if (t.temporada_optima) { %><span class="metric-chip">Temporada: <%= t.temporada_optima %></span><% } %>
                        <% if (t.dias_cosecha) { %><span class="metric-chip">Días: <%= t.dias_cosecha %></span><% } %>
                      </div>
                      <div class="flex gap-1 mt-1 opacity-0 group-hover/item:opacity-100 transition">
                        <button class="btn btn-outline btn-xs" data-edit="tipo-cultivo" data-id="<%= t.id_tipo_cultivo %>">Editar</button>
                        <button class="btn btn-danger btn-xs" data-delete="tipo-cultivo" data-id="<%= t.id_tipo_cultivo %>">X</button>
                      </div>
                    </div>
                  <% }) %>
                </div>
              <% } else { %>
                <div class="text-[11px] text-slate-400">Sin tipos registrados.</div>
              <% } %>
            </div>
          <% }) %>
        </div>
      </div>

      <div class="mb-4">
        <h3 class="text-xs font-semibold uppercase tracking-wider text-slate-500 mb-3">Insumos</h3>
        <div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3" id="insumosGrid">
          <% categoriasInsumo.forEach(cat => { const insCat = insumos.filter(i=> i.id_categoria_insumo === cat.id_categoria_insumo); %>
            <div class="glass-card rounded-xl p-5 flex flex-col gap-3 border border-slate-200/60 group" data-kind="insumo" data-name="<%= cat.nombre_categoria %>" data-entity="categoria-insumo" data-id="<%= cat.id_categoria_insumo %>">
              <div class="flex justify-between items-start">
                <div>
                  <h4 class="font-semibold text-slate-800 group-hover:text-indigo-600 transition"><%= cat.nombre_categoria %></h4>
                  <p class="text-[11px] text-slate-500 mt-1"><%= cat.descripcion || 'Sin descripción.' %></p>
                </div>
                <div class="flex flex-col items-end gap-2">
                  <span class="status-badge status-activa"><%= insCat.length %> insumos</span>
                  <div class="flex gap-1 opacity-0 group-hover:opacity-100 transition">
                    <button class="btn btn-outline btn-xs" data-edit="categoria-insumo" data-id="<%= cat.id_categoria_insumo %>">Editar</button>
                    <button class="btn btn-danger btn-xs" data-delete="categoria-insumo" data-id="<%= cat.id_categoria_insumo %>">X</button>
                  </div>
                </div>
              </div>
              <% if (insCat.length) { %>
                <div class="border border-slate-200/60 rounded-lg divide-y divide-slate-200 overflow-hidden bg-white/40 backdrop-blur-sm">
                  <% insCat.forEach(ins => { %>
                    <div class="p-3 flex flex-col gap-1 group/item" data-kind="insumo" data-name="<%= ins.nombre_insumo %>" data-entity="insumo" data-id="<%= ins.id_insumo %>">
                      <div class="flex justify-between items-center">
                        <span class="text-[12px] font-semibold text-slate-700"><%= ins.nombre_insumo %></span>
                        <% if (ins.fabricante) { %><span class="text-[10px] text-slate-500"><%= ins.fabricante %></span><% } %>
                      </div>
                      <% if (ins.descripcion) { %><p class="text-[10px] text-slate-500 leading-snug"><%= ins.descripcion.substring(0,140) %><%= ins.descripcion.length>140?'...':'' %></p><% } %>
                      <div class="flex flex-wrap gap-1 mt-1">
                        <% if (ins.tipo) { %><span class="metric-chip">Tipo: <%= ins.tipo %></span><% } %>
                      </div>
                      <div class="flex gap-1 mt-1 opacity-0 group-hover/item:opacity-100 transition">
                        <button class="btn btn-outline btn-xs" data-edit="insumo" data-id="<%= ins.id_insumo %>">Editar</button>
                        <button class="btn btn-danger btn-xs" data-delete="insumo" data-id="<%= ins.id_insumo %>">X</button>
                      </div>
                    </div>
                  <% }) %>
                </div>
              <% } else { %>
                <div class="text-[11px] text-slate-400">Sin insumos registrados.</div>
              <% } %>
            </div>
          <% }) %>
        </div>
      </div>

    </div>
    <aside class="rightbar">
      <div class="right-card">
        <h4 class="text-xs font-semibold tracking-wider text-slate-500 uppercase">Tips</h4>
        <p class="text-[11px] leading-relaxed">Usa el buscador para filtrar rápidamente cultivos o insumos. Próxima mejora: carga diferida de variedades.</p>
      </div>
    </aside>
  </div>
  <!-- Lógica de filtrado movida a app.js (catalogInit) -->
  <div id="modalHost" style="display:none"></div>
  <%- include('partial/footer') %>
</body>
</html>
